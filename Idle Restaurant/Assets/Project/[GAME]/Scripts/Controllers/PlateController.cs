using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlateController : MonoBehaviour
{/*
    List<IngredientsBaseSO> stackedObjects = new List<IngredientsBaseSO>();

    void StackFood(IngredientsBase food) 
    {
        stackedObjects.Add(food); // Add food to the plate's stack
        // Position the stacked food objects accordingly
    }
    void OnClick() 
    {
        // If the player is not holding anything and clicked on the plate,
        // pick up the stacked objects (if any)

        if (!holdingFood && IsPlateClicked()) 
        {
            // Pick up the stacked objects
            // Implement logic to pick up stacked objects with the plate
        }
    }*/
}
